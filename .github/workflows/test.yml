name: Testing

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Set up Python
        run: uv python install 3.12

      - name: Install dependencies
        # Sync project and install the 'dev' dependency group
        run: uv sync --group dev

      - name: ruff-check
        run: uv run ruff format --check src/analyze_dna/ tests/

      - name: pylint
        run: uv run pylint src/analyze_dna/

      - name: mypy
        run: uv run mypy src/analyze_dna/comprehensive_snp_database.py src/analyze_dna/traits_snp_database.py

      - name: pytest
        run: uv run pytest tests

